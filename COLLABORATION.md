# 🤝 คู่มือการทำงานร่วมกัน (Collaboration Guide)

หากเพื่อน Clone โปรเจกต์ไปแล้ว ให้ทำตามขั้นตอนดังนี้เพื่อให้ Backend (Hono + D1) ทำงานได้:

## 1. ติดตั้ง Dependencies
```bash
# ในโฟลเดอร์ root
cd client && npm install
cd ../server && npm install
```

## 2. ตั้งค่า Cloudflare (สำหรับเพื่อน)
เพื่อให้เพื่อนใช้ Database D1 ได้ มี 2 วิธี:

### วิธีที่ 1: ใช้ Database เดียวกัน (แนะนำสำหรับงานกลุ่ม)
1. คุณ (เจ้าของโปรเจกต์) ต้องเชิญเพื่อนเข้า **Cloudflare Account** (ไปที่ Cloudflare Dashboard > Manage Account > Members > Invite)
2. เพื่อนรันคำสั่ง:
   ```bash
   npx wrangler login
   ```
3. เพื่อนจะสามารถเข้าถึง D1 เดียวกันได้ทันที โดยไม่ต้องแก้ไข `database_id`

### วิธีที่ 2: ใช้ Database แยกคนละตัว
1. เพื่อนรันคำสั่งสร้าง Database ใหม่ของตัวเอง:
   ```bash
   npx wrangler d1 create baby-tracker-db
   ```
2. เพื่อนนำ `database_id` ที่ได้ไปทับในไฟล์ `wrangler.jsonc` (ระวังตอน Commit อย่าให้ทับของเพื่อน)

## 3. เตรียม Local Database
ไม่ว่าจะใช้วิธีไหน เพื่อนต้องสร้างไฟล์ Database จำลองในเครื่องตัวเองก่อน:
```bash
cd server
npx wrangler d1 migrations apply baby-tracker-db --local
```

## 4. รันโปรเจกต์
```bash
# รัน Backend
npm run dev

# รัน Frontend (ในอีก Terminal)
cd ../client
npm run dev
```

---

## 🚀 ขั้นตอนถัดไปสำหรับโปรเจกต์
1. **ทำ API Logs:** จัดการเรื่องการให้นม, ผ้าอ้อม, การนอน
2. **เชื่อม Frontend:** แก้ไขให้ UI เรียกใช้ API แทน Mock data
